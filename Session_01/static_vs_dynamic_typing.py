# Dynamic Typing means like in C/C++ we used to define the variables previously that, we need int as a, String as name but on the other hand in python the interpreter is doing that for us, It recognizes the variable by itself so that's what we are calling here as dynamic typing.

# Static typing: Where we tell data type before like in C or C++ that int a = 1.