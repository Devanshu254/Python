s = 'hello world'
# s[0] = 'H' -> This will throw an error, strings are immutable in python.
# Strings can not be edited.

# Deletion in string. 
# There is one del function.
del s # It's getting unreferenced.
print(s)
